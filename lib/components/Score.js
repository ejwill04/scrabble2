import React, { Component } from 'react';

export default (props) => {
  const { usersScore } = props;
  return (
    <div>
      Score: {usersScore}
    </div>
  );
};


// turn off eslint until saving
// window management debbie
